@{
    ViewData["Title"] = "Главная страница";
}

<style>
    body {
    background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
    color: #f0f0f0;
    font-family: 'Poppins', sans-serif;
    margin: 0;
    min-height: 100vh;
    /* Убираем flex, чтобы не ломать футер */
    /* display: flex; */
    /* flex-direction: column; */
    /* justify-content: center; */
    /* align-items: center; */
    /* padding перенесём внутрь .container */
}

    .container {
    max-width: 960px;
    width: 100%;
    text-align: center;
    padding: 2rem 1rem; /* Отступы теперь здесь */
    margin: 0 auto;
}

    h1 {
        font-weight: 800;
        font-size: 4rem;
        margin-bottom: 1rem;
        letter-spacing: 0.07em;
        text-shadow: 0 0 18px #1DB954aa;
        color: #d7f7d7;
        user-select: none;
        animation: fadeInDown 1s ease forwards;
        opacity: 0;
    }

    p.lead {
        font-size: 1.3rem;
        color: #b0d8d8cc;
        max-width: 720px;
        margin: 0 auto 3rem;
        line-height: 1.65;
        user-select: none;
        animation: fadeInUp 1s ease 0.5s forwards;
        opacity: 0;
    }

    .features {
        display: flex;
        gap: 2.2rem;
        justify-content: center;
        flex-wrap: wrap;
        margin-bottom: 4rem;
        animation: fadeIn 1s ease 1s forwards;
        opacity: 0;
    }

    .feature-card {
        background: rgba(255, 255, 255, 0.07);
        border-radius: 24px;
        padding: 2.3rem 2rem;
        width: 280px;
        box-shadow:
            0 8px 32px 0 rgba(31, 38, 135, 0.37),
            0 0 0 1px rgba(255, 255, 255, 0.1);
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.15);
        transition: transform 0.35s ease, box-shadow 0.35s ease;
        cursor: default;
        color: #e1f0e8;
        user-select: none;
    }
    .feature-card:hover {
        transform: translateY(-14px);
        box-shadow:
            0 15px 40px 0 rgba(29, 185, 84, 0.65),
            0 0 0 1px rgba(29, 185, 84, 0.9);
        border-color: #1DB954dd;
        color: #c0e8b8;
    }

    .feature-icon {
        width: 56px;
        height: 56px;
        margin: 0 auto 1.3rem;
        fill: #1DB954cc;
        filter: drop-shadow(0 0 4px #1DB954bb);
        transition: fill 0.3s ease;
    }

    .feature-card:hover .feature-icon {
        fill: #1DB954ff;
        filter: drop-shadow(0 0 8px #1DB954ff);
    }

    .feature-title {
        font-weight: 700;
        font-size: 1.5rem;
        margin-bottom: 1rem;
        user-select: text;
    }

    .feature-text {
        font-size: 1rem;
        line-height: 1.5;
        color: #aad4a7cc;
        user-select: text;
    }

    .user-info {
        background: rgba(29, 185, 84, 0.15);
        border-radius: 15px;
        padding: 1.7rem 2.5rem;
        margin-bottom: 3rem;
        max-width: 600px;
        margin-left: auto;
        margin-right: auto;
        text-align: center;
        font-weight: 600;
        color: #d4f5d4;
        box-shadow: 0 0 30px #1DB954bb;
        user-select: none;
        animation: fadeInUp 1s ease 1.8s forwards;
        opacity: 0;
    }

    .btn-primary-custom {
        background-color: #1DB954;
        border: none;
        font-weight: 700;
        padding: 0.9rem 3rem;
        font-size: 1.3rem;
        border-radius: 50px;
        box-shadow: 0 8px 25px #1DB954cc;
        transition: background-color 0.3s ease, box-shadow 0.3s ease, transform 0.3s ease;
        cursor: pointer;
        color: white;
        user-select: none;
        animation: fadeInUp 1s ease 2.2s forwards;
        opacity: 0;
        display: inline-block;
        text-decoration: none;
    }
    .btn-primary-custom:hover {
        background-color: #16a443;
        box-shadow: 0 12px 35px #149e3dff;
        transform: scale(1.05);
        text-decoration: none;
        color: white;
    }

    .btn-login-register {
        font-weight: 600;
        font-size: 1.1rem;
        color: #8dd3fb;
        margin: 0 1rem;
        text-decoration: none;
        user-select: none;
        transition: color 0.3s ease;
    }
    .btn-login-register:hover {
        color: #1DB954;
        text-decoration: underline;
    }



    /* Animations */
    @@keyframes fadeInDown {
        0% {opacity: 0; transform: translateY(-20px);}
        100% {opacity: 1; transform: translateY(0);}
    }
    @@keyframes fadeInUp {
        0% {opacity: 0; transform: translateY(20px);}
        100% {opacity: 1; transform: translateY(0);}
    }
    @@keyframes fadeIn {
        0% {opacity: 0;}
        100% {opacity: 1;}
    }
</style>

<div class="container">
    <h1>CyberSecLabPlatform</h1>
    <p class="lead">
        Погрузитесь в мир кибербезопасности с помощью интерактивных практических сценариев, созданных для студентов и специалистов.
        Учитесь, оценивайте знания и совершенствуйте навыки защиты цифровых систем в безопасной и удобной среде.
    </p>

    <div class="features">
        <div class="feature-card" role="region" aria-label="Реальные сценарии атак">
            <svg class="feature-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" aria-hidden="true" focusable="false">
                <path d="M32 2L6 12v14c0 13 10 23 26 26 16-3 26-13 26-26V12L32 2zM32 38a6 6 0 1 1 0-12 6 6 0 0 1 0 12z" />
            </svg>
            <div class="feature-title">Реальные сценарии атак</div>
            <div class="feature-text">
                Проходите имитации настоящих угроз и учитесь быстро и эффективно реагировать на инциденты.
            </div>
        </div>

        <div class="feature-card" role="region" aria-label="Аналитика и отчёты">
            <svg class="feature-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" aria-hidden="true" focusable="false">
                <path d="M8 50h48v4H8zm8-14h32v4H16zm-8-14h48v4H8zM12 12h40v4H12z" />
            </svg>
            <div class="feature-title">Аналитика и отчёты</div>
            <div class="feature-text">
                Аналитики и преподаватели получают глубокий обзор прогресса и успехов студентов, чтобы оптимизировать обучение.
            </div>
        </div>

        <div class="feature-card" role="region" aria-label="Доступность 24/7">
            <svg class="feature-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" aria-hidden="true" focusable="false">
                <circle cx="32" cy="32" r="28" stroke="#1DB954" stroke-width="4" fill="none"/>
                <path d="M32 14v20l14 8" stroke="#1DB954" stroke-width="4" fill="none" stroke-linecap="round"/>
            </svg>
            <div class="feature-title">Доступность 24/7</div>
            <div class="feature-text">
                Учитесь и практикуйтесь в любое удобное время и с любого устройства — платформа всегда с вами.
            </div>
        </div>
    </div>

    @if (User.Identity.IsAuthenticated)
    {
        <div class="user-info" role="region" aria-live="polite" aria-atomic="true">
            Вы вошли как <strong>@User.Identity.Name</strong><br />
            Роль: <strong>@string.Join(", ", User.Claims.Where(c => c.Type == System.Security.Claims.ClaimTypes.Role).Select(c => c.Value))</strong>
        </div>

        <div>
            @if (User.IsInRole("Admin"))
            {
                <a href="/Admin" class="btn-primary-custom" role="button" aria-label="Личный кабинет Администратора">Личный кабинет Администратора</a>
            }
            else if (User.IsInRole("Analyst"))
            {
                <a href="/Analyst" class="btn-primary-custom" role="button" aria-label="Личный кабинет Аналитика">Личный кабинет Аналитика</a>
            }
            else if (User.IsInRole("Student"))
            {
                <a href="/Student" class="btn-primary-custom" role="button" aria-label="Личный кабинет Студента">Личный кабинет Студента</a>
            }
        </div>
    }
    else
    {
        <p class="mt-4 fs-5" role="region" aria-live="polite">
            <a href="/Account/Login" class="btn-login-register" aria-label="Войти">Войти</a>
            или
            <a href="/Account/Register" class="btn-login-register" aria-label="Зарегистрироваться">Зарегистрироваться</a>,
            чтобы получить доступ к интерактивным курсам и практическим заданиям.
        </p>
    }
</div>
